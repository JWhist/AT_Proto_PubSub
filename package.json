{
  "name": "atp-test",
  "version": "1.0.0",
  "description": "Testing firehose read and filter of events",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@atproto/api": "^0.17.0",
    "@atproto/identity": "^0.4.9",
    "@atproto/sync": "^0.1.35",
    "@atproto/xrpc-server": "^0.9.5",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
